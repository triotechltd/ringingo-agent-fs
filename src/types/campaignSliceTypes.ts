export interface ResumeCampaignTypes {
    campaign_uuid: string;
    sip_uri: string;
    username: string;
    campaign_mode: string;
}

export interface PauseCampaignTypes {
    campaign_uuid: string;
    campaign_mode: string;
}
