/* ============================== MINIMAL LAYOUT ============================== */

const MinimalLayout = ({ children }: { children: React.ReactNode }) => {
    return <>{children}</>
}

export default MinimalLayout